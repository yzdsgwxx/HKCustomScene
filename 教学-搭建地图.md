# 教学 · 搭建地图（房间）完整笔记

> 适用工程：`D:\HKModding\hkmod-custom-scene`（空洞骑士自定义场景 Mod，API v77 / SFCore 1.5.16.2）
> 记录时间：2026-09-26 · 配套文档：`教学-从零理解.md`、`教学-阶段三-详细操作.md`、`handoff.md`
> 这份笔记把「从新建一个空场景 → 游戏里能走进去、有相机、有门、有椅子和小怪」的**每一步操作、每个字段、
> 成功信号、踩坑点**收在一处。**先看 §1 的五条硬规矩**，然后按 §2 顺序做。

---

## 0. 一分钟总览

```
[Unity] 新建场景 → 摆地形 → mesh 生成碰撞 → 加 _Managers/__Initializer → 设 AssetBundle 名
   ↓
[C#]    Consts/RoomNames.cs 登记一行（Width/Height 必须 = 地形实测跨度）
   ↓
[Unity] 摆出入口门（TransitionPoint + 触发器 + respawnMarker）→ 摆椅子 / 小怪 / 区域名
   ↓
[脚本]  tools\打包测试.cmd（打 AssetBundle → 拷包 → 编 FinalMod → 装 dll → 重启游戏）
   ↓
[游戏]  看 ModLog.txt 的「成功信号」（§6）
```

每一步的产物：

| 阶段 | 产物 |
|---|---|
| Unity 场景 | `Assets/Scenes/HKCS_RoomXX.unity`，**AssetBundle 名 = `hkcs_scenes`** |
| 房间表 | `FinalMod/Consts/RoomNames.cs` 里一行 `RoomDef` |
| 路由 | 复用 Room01 的写法（或给 `ResolveRedirect` 加一条规则） |
| 构建 | `FinalMod\Resources\hkcs_scenes` + 新的 `HKCustomSceneMod.dll` |

---

## 1. 五条硬规矩（违反必出怪问题）

| # | 规矩 | 违反会怎样 |
|---|---|---|
| **1** | **地形必须从 (0,0) 开始铺** | 原版把相机左边界写死 `x = 14.6`、下边界写死 `y = 8.3`（= 半个屏幕）⇒ `x<0 / y<0` 的部分玩家**永远看不到**。`TileMapFix` 会在日志里打红字提醒 |
| **2** | **`RoomNames.cs` 里的 Width/Height 必须 = 地形网格实测跨度** | 相机边界 = `Width-14.6` / `Height-8.3`。写大了：走到边上能看见**房间外的黑**；写小了：房间看不全 |
| **3** | **地形必须在 Layer 8（Terrain）且有 Collider** | 没碰撞：骑士会掉出地图；小怪自动落地会失败（日志：`往下 50 格没打到地形`） |
| **4** | **每个房间都要登记进 `Consts/RoomNames.cs`** | 不登记 ⇒ 不补 TileMap、不设相机边界 ⇒ 重现 `CameraController.GetTilemapInfo` 那串 NRE（**相机锁死、不跟随、地图坏**）。线上排查时看到相机"锁住"先查这条 |
| **5** | **场景资源的 AssetBundle 名必须是 `hkcs_scenes`** | 包里没这个场景 ⇒ 启动日志 `[HKCS] 场景包里找不到房间 XXX` |

**还有一条排序铁律**（§4 详解）：

> **骑士自己的渲染器是 `Default` 层 / order 0 / z = 0** ⇒ 道具的 **z 不能小于 0**（否则会被画到骑士前面，把他整个盖住）。
> 同时**不透明地形会写深度**，道具放在它后面（z 更大）会被地形挡。
> ⚠ `TagManager` 里那个叫 `Player` 的排序图层，**骑士并不用它** —— 别想当然。

---

## 2. 逐步操作

### A. 场景与地形（第 1–5 步）

**1｜新建场景**
`Assets/Scenes/` 右键 → Create → Scene，命名 `HKCS_RoomXX`（**必须和房间表里的 `Scene` 完全一致**）。

**2｜摆地形**
现在地形用的是 `Assets/Meshes/*.obj` 之类的 mesh（以后可以换成原版图集拼的 sprites）。
- 地形根物体 **Transform 归零到 (0,0,0)**（规矩 1）；
- 地形（含所有子物体）**Layer 设成 `Terrain`**（Layer 8，规矩 3）。

**3｜生成碰撞**
选中带 `MeshFilter` 的物体 → 组件右侧 `⋮` → **Create Collision**（工程里的 `MeshCollisionCreator`），生成一个 `PolygonCollider2D`。

**4｜材质**
当前用 `Assets/Materials/Terrain.mat`（**内置 Standard，不透明**）。先记住它的两个后果：
- 它**会写深度** ⇒ 透明道具落在它后面会被它挡（§4）；
- 想彻底摆脱这个限制：把地形换成**透明材质**（如 `Sprites/Default`）并按 §4 的方案 B 设 order。

**5｜量出房间尺寸（别猜）**
进一次游戏，日志里会有：
```
[HKCS] HKCS_RoomXX 地形网格实测：x[0.00 ~ 60.00]（宽 60.00）、y[0.00 ~ 17.00]（高 17.00）
```
⇒ **Width = 60、Height = 17**。（没进过游戏也可以先用 Unity 里的 mesh bounds 估。）

---

### B. 让 mod 认识这个房间（第 6–10 步）

**6｜登记房间**（`FinalMod/Consts/RoomNames.cs`）
```csharp
public static readonly List<RoomDef> All = new List<RoomDef>
{
    //                                 scene    w    h   fromPrev  toNext    title
    new RoomDef(Room01, 60f, 17f, "left1",  "right1", true),
    new RoomDef(Room02, 60f, 17f, "left1",  "right1", false),   // ← 新增这一行
};
```
- `w / h` = 第 5 步实测值（规矩 2）；
- `fromPrev` = 从上一个房间进来时落在哪个门（门名）；
- `toNext` = 通往下一个房间的门名；
- `ShowAreaTitle` 只有**整个区域的第一间房**填 `true`。

**7｜加 `_Managers` 和 `__Initializer`**
菜单 **Tools/HKCS/生成 __Initializer**，它会建好 `_Managers`（空）+ `__Initializer`，并挂上：
`PatchAreaTitleController` + `PatchPlayMakerManager` + SFCore 的 `SceneManagerPatcher`（区域名 / 音乐氛围 / 场景尺寸都靠这三个）。

**8｜设 AssetBundle 名**（规矩 5）
Project 窗口选中这个 `.unity` → Inspector **最底部** AssetBundle → 填 `hkcs_scenes`（和 Room01 一样）。

**9｜（可选）地图贴图**
把 SFCore 的 `SceneMapPatcher` 挂在地形根物体上，`tex` 给 `Assets/Materials/black.png`（纯黑，占位）。

**10｜TileMap 不用自己加**
`FinalMod/TileMapFix.cs` 会在进房间时自动往**场景根**补一个空的 `tk2dTileMap`（tag `TileMap`），
因为原版 `GameManager.tilemap` 为 null 会让相机 NRE。日志：
```
[HKCS] HKCS_RoomXX 里没有 tk2dTileMap → 已补一个空替身（tag=TileMap，60x17）
```

---

### C. 出入口（第 11–15 步）

**11｜门名决定落点**（反编译 `HeroController.EnterScene` 得到）

| 门名含 | 落点 |
|---|---|
| `left` | `x = 门.x + entryOffset.x + 2`，贴地，面朝右 |
| `right` | `x = 门.x + entryOffset.x − 2`，贴地，面朝左 |
| `bot` | `x = 门.x`，`y = 门.y + entryOffset.y + 3`（从上方落下）|
| `top` | **= 门原地，没有任何偏移** ⚠ |
| `door` | 门位置贴地 |

⚠ **`top*` 名字的门不要给它"以自身为中心"的触发框**，否则落点就在框里 ⇒ 一落地就自弹回原场景（0.17 踩过）。

**12｜门的组件**
`TransitionPoint` + `BoxCollider2D`：
- **Is Trigger ✓**（忘了勾会变成一堵实心墙）；
- `Offset = (0,0)`、`Size = (1,4)`（**手输**，别用 Edit Collider 拖，拖出来的框会偏）；
- 填 `targetScene`、`entryPoint`、`alwaysEnterLeft` / `alwaysEnterRight`。

**13｜门的 `respawnMarker`**
门下面挂一个 `HazardRespawnMarker` 并连到 `respawnMarker` 字段。空着会在进场时报
`PlayerData.SetHazardRespawn` 的 NRE（0.13 踩过）。

**14｜直接用现成预制体**
`Assets/Prefabs/Door_Left.prefab` / `Door_Right.prefab`：场景里只拖 x/y，**z 在预制体里设**（别在场景实例上改）。

**15｜路由写在 C# 里**
`CustomSceneMod.ResolveRedirect` 目前只有两条规则：
```csharp
(Town          → Crossroads_01) ⇒ HKCS_Room01 : left1     // 跳井进来
(Crossroads_01 → Town)          ⇒ HKCS_Room01 : right1    // 从井底爬上来
```
- 从**原版场景**进你的房间：改这张表（或复用 Room01 的名字）；
- **房间之间**的门：目前要在 Unity 里手填 `targetScene / entryPoint`（`SceneChanger.BuildRoomLinks` 是死代码，没人调用）。

---

### D. 摆件（第 16–19 步）

**16｜长椅（存档点）**
拖 `Assets/Prefabs/Bench.prefab` → 摆到**骑士站的那块地面**上。字段：
- `BenchName`：存档用的重生点名字（**同一房间多把椅子别重名**）；
- `YOffset`：微调（正数往上）。高度会自动按长椅自带的 trigger 对齐，日志会给实测边界：
  `[HKCS] 长椅本体(sprite=town_bench) 世界边界 …；摆放点 y=… ⇒ 脚相对摆放点 -0.113`。

**17｜小怪**
拖 `Assets/Prefabs/Enemy.prefab` → 摆在想要的位置。字段：

| 字段 | 填什么 | 说明 |
|---|---|---|
| `Kind` | `ZombieRunner` / `Fly` / `Crawler` / `Climber` | 四种十字路小怪 |
| `Id` | **留空** | 留空 = 用摆放点物体名当唯一编号（存档击杀记录用）|
| （z） | 在 `Enemy.prefab` 里设 | 见 §4：**别小于 0**，否则会盖住骑士 |

行为要点：
- **除苍蝇外会自动落地**：往下打 50 格射线找 `Terrain` 层，把碰撞盒底边放到地面上（日志 `落地：摆放点 y=… → y=…`）；
- `Climber` 是 **kinematic + 零重力**，靠"贴着表面"活动 ⇒ **必须贴着地面/墙面摆**，否则会在空气里爬；
- `Fly` 也是零重力，**放多高就飞多高**（不落地）。

**18｜区域名**
- 只有**区域第一间房**把 `PatchAreaTitleController.SubArea` 设 `false`；
- `AreaEvent` / `VisitedBool` 三个地方必须**完全一致**：Unity 组件字段、`Consts/RoomNames.cs` 的 `AreaInfo`、`SettingsClass` 的字段名；
- 日志里 `[HKCS] 找不到标题物体 Area Title` 是**已知无害**（标题照样会弹）。

**19｜（可选）相机锁区**
要分区锁镜头时，放一个空物体挂 `CameraLockArea`（工程里有 `CameraLockAreaEditor` 可视化），
原版就是靠它刷新相机的锁定框。

> **编辑器预览（摆件时很好用）**：`HKCSPlacementPreview` 会给每个 `PatchBench` / `PatchEnemy` 在场景里
> 生成一个**真实尺寸的预览物体**（`HideFlags.DontSave`，不进场景文件、不进 bundle、游戏里不存在），
> 缩放跟随、位置按游戏里的对齐规则算，所以"编辑器里看着贴地 = 游戏里贴地"。
> 开/关：菜单 **`工具/HKCS 预览/开关预览物体`**；层级里 `PatchBench`/`PatchEnemy` 图标由
> `HKCSPlacementGizmos` 画。⚠ 它只是画给你看，**不影响打包结果**。

---

### E. 打包与验证（第 20–21 步）

**20｜打包**
- 只改了 Unity 场景/美术：双击 **`tools\打包测试.cmd`**；
- 改了**壳工程**（`MonoBehaviours\` 里的字段/枚举）：先 **`tools\更新壳工程.cmd`**，再 `打包测试.cmd`；
- Unity 里也能点：菜单 **`HKModCustomSceneTool/打包测试`**（= 同一个脚本）。

`打包测试` 内部五步：打 AssetBundle → 拷进 `FinalMod\Resources\` → `dotnet build` FinalMod →
装 dll 到 `Managed\Mods\CustomScene\` → 重启游戏。日志在 `tools\_log\`。

**21｜进游戏看日志**（见 §6）

---

## 3. 美术从哪来（解包素材）

**全量解包结果**（**在仓库外**，不进 git）：

```
D:\HKModding\_hkassets\map_art\Crossroads_01\<原 .assets 文件名>\<贴图名>.png
D:\HKModding\_hkassets\map_art\Crossroads_01\manifest.json     ← 来源 / 尺寸 / 路径清单
```

- 规模：`level37`（= `Crossroads/Crossroads_01`）引用的 12 个 `.assets` 里的全部贴图/精灵 = **4245 张 / 166 MB**；
- 重新导出（换区域就换 level 号）：`python D:\HKModding\_hkassets\export_area_art.py 43 Crossroads_07`
- 查 levelN ↔ 场景名：`python D:\HKModding\_hkassets\scene_map.py Crossroads`

**各目录大致内容**

| 目录 | 内容 |
|---|---|
| `sharedassets9` | 十字路大图集（`atlas0/1/2`：地形/场景 + 带角僵尸的行走/死亡帧）|
| `sharedassets37` | 井口一带（`atlas0` = 爬虫图集、`sactx-*Crossroads_Well*`、`Well`、`Back_Highway`）|
| `sharedassets33` | 复仇苍蝇图集（`atlas0`）|
| `sharedassets7` | 德特茅斯 / 墓地 / 感染体 |
| `sharedassets6` | 王国关口 / 教程区（`Tut_*`）|
| `sharedassets19` | 法术特效 |
| `sharedassets27` | 真菌 / 蜂巢 |
| `resources` | 图鉴图标、地图图标、UI、存档图 |

**导入到工程**：把要用的 png 拷到 `UnityProject\HKModCustomScene\Assets\Art\...` 即可（Unity 会自动导入）。
> ⚠ 之前导入过一版 4.7 MB 的精选（`Assets\Art\Crossroads\{Enemies,Environment}`），在 2026-09-26 的回滚中被清理掉了，
> 需要就按上面的命令重新导出 + 拷贝。

**已知限制（搭地图时要知道）**

1. 小怪的 `MeshFilter.m_Mesh` 在场景文件里是**空的**（网格运行时才由 tk2d 赋）⇒ **没法靠 UV 自动裁单帧**；
2. 图集（`atlas0.png` 这类整张 DXT5）的**帧格不是均匀网格** ⇒ 想用单帧得**手动切**（Unity Sprite Editor），
   或者干脆**整张当背景/装饰**；
3. 这些图集是**美术源**，不是"能直接拖进场景就成型的地形"。

**版权**：这些是 Team Cherry 的资产，只适合**本地做 mod**，**不要公开分发**。不想让它们进 git 就写进 `.gitignore` 或用 git-lfs。

---

## 4. 排序：z / Sorting Layer / Order 到底怎么定

**Unity 的 2D 排序是两级**：

1. **跨图层**：按 Sorting Layer 在 `ProjectSettings/TagManager.asset` 里的顺序，**越靠后越前面**；同层内按 Order in Layer（大的在前）。
2. **同层 + 同 order 内**：按**到相机的距离（z）**排。

本工程的 14 个图层（顺序 = 绘制顺序，**后面的在前**）：
```
Default → Far BG 2 → Far BG 1 → Mid BG → Immediate BG → Actors → Player
        → Tiles → MID Dressing → Immediate FG → Far FG → Vignette → Over → HUD
```

**两条铁律（实测）**

- **骑士 = `Default` / order 0 / z = 0**（不是 `Player` 层！）⇒ 道具 **z ≥ 0** 才会在骑士**后面**；
- **不透明物体会先画并写深度** ⇒ 透明道具放在不透明地形**后面**（z 更大）会被地形**挡掉**（跟图层无关）。

**两套自洽摆法（选一套，别混用）**

| 方案 | 地形 | 长椅 / 小怪 | 结果 |
|---|---|---|---|
| **A（现状）** | `Default`，不透明 Standard，z = 0（不动）| 也留 `Default`；长椅 z = **0.02**、小怪 z ≈ **0.02**（在各自预制体里设）| 骑士 z=0 更小 ⇒ 永远在道具前面 ✓；⚠ 道具 z>0 时若背后正好是不透明地形，会被地形挡 |
| **B（想真正用 order 分层）** | 材质换**透明**（ZWrite Off，如 `Sprites/Default`），同层 `Default`，**Order in Layer = −1** | 同层 `Default`，order 0，z 用 0.02 这种小偏移 | 没有深度遮挡问题，顺序完全由 order 说话 ✓，骑士仍在道具前面 ✓ |

⚠ **不要**把道具放到 `Actors`：它在 `Default` **之后**，而骑士在 `Default` ⇒ 道具会**盖住骑士**（2026-09-26 实测踩过）。

---

## 5. 相机与房间尺寸速查

```
xLimit = Width  - 14.6        yLimit = Height - 8.3
相机中心范围 = [14.6, xLimit] × [8.3, yLimit]
相机视野 = 29.2 × 16.6（14.6 / 8.3 就是半个屏幕，原版写死的）
```

⇒ 相机视野**恰好**覆盖 `x[0, Width] × y[0, Height]`。这就是"地形必须从 (0,0) 铺、Width 必须等于地形宽度"的原因。

---

## 6. 日志对照表（成功信号 / 错误信号）

日志位置：`%USERPROFILE%\AppData\LocalLow\Team Cherry\Hollow Knight\ModLog.txt`（`Player.log` 里也有 `[HKCS]` 行）

**应该出现（缺哪条查哪条）**

| 日志 | 含义 |
|---|---|
| `[HKCS] 场景包已加载，含 N 个场景：…` | AssetBundle 进去了（第 8 步）|
| `[HKCS] <房间> 里没有 tk2dTileMap → 已补一个空替身（tag=TileMap，WxH）` | TileMap 补上了（相机不会再 NRE）|
| `[HKCS] <房间> 地形网格实测：x[…] y[…]` | 用来核对房间表尺寸（规矩 2）|
| `[HKCS] 相机范围设为 xLimit=… yLimit=…` | 相机边界已生效 |
| `[HKCS] 放了长椅 …` / `[HKCS] 放了小怪 …` | 摆件成功 |
| `[HKCS] <怪> 落地：摆放点 y=… → y=…（地面 y=…）` | 小怪自动落地成功 |
| `[HKCS] <房间> 尺寸设为 60x17` | 房间表被读到 |

**不该出现（出现就按 §7 处理）**

| 日志 | 说明 |
|---|---|
| `Failed to find tilemap in <房间> entirely.` | TileMap 没补上（相机要 NRE）|
| `CameraController.GetTilemapInfo` 的 `NullReferenceException` | 同上：`GameManager.tilemap == null` |
| `[HKCS] 场景包里找不到房间 XXX` | 该场景没进 bundle（AssetBundle 名没填 / 名字不一致 / 这间房还没做）|
| `[HKCS] 地形没有从 (0,0) 开始铺` | 规矩 1 |
| `[HKCS] 房间表登记 A，地形实测却是 B` | 规矩 2 |
| `[HKCS] <怪> 落地失败：往下 50 格没打到地形` | 摆放点在地面下方/房间外，或地形没碰撞/不在 Terrain 层 |

---

## 7. 排查总表（症状 → 原因 → 处理）

| 症状 | 最可能的原因 | 处理 |
|---|---|---|
| 相机**锁死、不居中、不跟随** | 这个房间没登记进 `RoomNames.cs`（或名字不一致）⇒ `tilemap` 为 null | 登记一行（规矩 4），看日志有没有"已补一个空替身" |
| 走到边上**看见房间外的黑** | `Width/Height` 比地形大（或地形没从 0 开始） | 按日志实测值改房间表 |
| 视野里 x<0 / y<0 那部分**永远看不到** | 地形没从 (0,0) 铺 | 地形根物体 Transform 归零 |
| **骑士被盖住 / 看不见人** | 道具 z < 0，或道具被放到 `Default` 之后的层（如 `Actors`） | 道具 z ≥ 0、留 `Default`（§4 方案 A） |
| 道具**被地形挡住** | 地形不透明会写深度 | 道具 z ≤ 地形 z，或地形换透明材质（§4 方案 B） |
| 小怪**在空气里爬** | `Climber` 是 kinematic，没贴着表面 | 摆放点贴着地面/墙面；看日志"落地"行 |
| 小怪**掉下去 / 不见了** | 摆放点下方没有碰撞体 | 地形要有 `PolygonCollider2D` 且在 `Terrain` 层 |
| 椅子**陷进地形 / 浮在空中** | 高度对齐 | 看日志"长椅本体…脚相对摆放点 -0.113"，用 `YOffset` 补齐 |
| **走出房间没反应** | 门的 `targetScene/entryPoint` 没填、`Is Trigger` 没勾、路由没写 | §2 第 12 / 15 步 |
| 一落地就**被弹回原场景** | `top*` 门带了以自身为中心的触发框 | §2 第 11 步 |
| 进场报 `PlayerData.SetHazardRespawn` NRE | 门的 `respawnMarker` 空着 | 挂 `HazardRespawnMarker` |
| 地图界面（按 Tab）坏 | `GameMap` 需要 `tilemap`；或 `SetManualTilemap` 没跑 | 看日志 TileMap 那条；房间表尺寸别为 0 |
| 改了壳工程（字段/枚举）后 Unity 报"类型找不到" | Unity 里那份壳 dll 还是旧的 | 跑 `tools\更新壳工程.cmd` |
| 预览图标/脚本改了没生效 | Unity 没重编 Editor 程序集 | **退出 Unity → 删 `Library\ScriptAssemblies\*` → 重启** |

---

## 8. 速查卡（新增一间房的 checklist）

```
[ ] 1  场景建在 Assets/Scenes/，名字 = 房间表里的 Scene
[ ] 2  地形根物体 Transform = (0,0,0)，所有地形 Layer = Terrain(8)
[ ] 3  MeshFilter ⋮ → Create Collision（每个 mesh 都要）
[ ] 4  地形材质确认（不透明 Standard 的话记住 §4 的限制）
[ ] 5  进一次游戏，抄下「地形网格实测」的宽高
[ ] 6  Consts/RoomNames.cs 加一行 RoomDef（Width/Height = 第 5 步）
[ ] 7  菜单 Tools/HKCS/生成 __Initializer（_Managers + __Initializer 三个组件）
[ ] 8  选中 .unity → Inspector 最底部 AssetBundle = hkcs_scenes
[ ] 9  出入口：TransitionPoint + BoxCollider2D(IsTrigger/Offset 0/Size 1x4) + targetScene/entryPoint
[ ] 10 出入口：挂 HazardRespawnMarker 并连到 respawnMarker
[ ] 11 摆 Bench.prefab / Enemy.prefab 的实例（z 在预制体里设，别 <0）
[ ] 12 区域第一间房才 SubArea=false；AreaEvent/VisitedBool 三处一致
[ ] 13 双击 tools\打包测试.cmd（改了壳工程就先 tools\更新壳工程.cmd）
[ ] 14 进游戏看 ModLog：TileMap / 地形实测 / 相机范围 / 放了… 四类行都在 = 成功
```

---

## 9. 迷宫入口 / 出口：灵魂怪 + 密码锁

这两个是"进/出迷宫"的机关，都**不需要门**，走的是"死亡式表现"（黑屏 + 在椅子上醒来）。
两者最终都用同一套：`PlayerData.SetBenchRespawn(椅子, 场景, true)` → `GameManager.ReadyForRespawn(false)`。
⚠ 这条路**不是真死亡**：不掉影子、不掉钱（真要死亡动画得用 `HeroController.Die()`，副作用很大）。

### 9.1 迷宫**入口**：可梦钉抽的灵魂怪（`PatchGhost`）

**两种用法**（同一个组件）：
- **就地生成**：勾不勾 `SpawnInVanillaScene` 都不影响，把它摆在我们自己的场景里 ⇒ 在这个场景的摆放点生成；
- **注入原版场景**（勾上 `SpawnInVanillaScene`）：本物体只当"配置卡"（摆在**我们自己的场景**里就行），
  运行时往 `SpawnScene`（默认 `Town` = 德特茅斯）的 `SpawnPosition` 坐标注入一只灵魂怪；
  **原版场景每次加载都会重造**（场景一卸载就销毁，不会跑到别的场景去）。
  - `SpawnScene` = 原版场景名；`SpawnPosition` = 世界坐标（默认 `(215.64, 8.33)` = 德特茅斯掘墓者右边 4 格；
    原版那只 `_NPCs/Gravedigger NPC` 实测在 `(211.64, 8.33)`）；
  - `UseVanillaLook` = true 时**克隆原版掘墓者的外观**（精灵/光晕/粒子/飘动，长得一模一样）。

摆法（就地生成）：空物体 + `PatchGhost` → 填 `GhostSprite`（灵魂贴图）/ `DialogueText`（梦钉文本）/
`TargetScene`（迷宫房间）/ `TargetBench`（迷宫里那把椅子的 `BenchName`）。
- 它运行时会自己造一只灵魂怪：根物体（层 `13 Hero Detector`）+ 子物体 `Dreamnail Hit`
  （层 **`19 Interactive Object`**、Trigger）+ `EnemyDreamnailReaction` ⇒ **能被梦钉抽**；
- 抽了**不消失**（没有原版那段"消失"FSM），延迟 `Delay` 秒后把你送到目标椅子上；
- 原理（反编译）：梦钉命中走英雄 FSM 的 `SendDreamImpact` → `EnemyDreamnailReaction.RecieveDreamImpact()`
  → 显示 `Enemy Dream Msg` 对话框；文本由本 mod 的 `LanguageGetHook` 按
  `HKCS_GHOST_<摆放点名>` 前缀提供 ⇒ **编辑器里填什么就显示什么**。

### 9.2 迷宫**出口**：密码锁区域（`PatchComboLock`）
摆法：在出口摆一个空物体 + `PatchComboLock`，光效/雕像美术自己往上摆；选中它时 Scene 视图会画出判定盒。
- 密码 = **六段动作**：`AttackUp`（上攻击）→ `AttackDown`（下攻击）→ `AttackLeft` → `AttackRight`
  → `JumpLeft`（面向左跳）→ `JumpRight`；每段次数填 **0 = 跳过该段**。
  ⚠ **下攻击只算"玩家在空中往下劈"那一下**（地面下劈不计入）；上/下攻击**不分左右**。
- 其它字段：`ZoneSize`（判定范围）、`RequireOrder`（默认 true = 必须按上面顺序）、`ResetOnWrong`、
  `ResetOnLeave`（默认 true：离开范围清零）、`AnywhereInScene`（默认 false；**勾上 = 整场景任何地方做完都算**，
  做"后门/保险"用）、`Delay`、`TargetScene` / `TargetBench`（默认 `Town` / `RestBench` = 德特茅斯长椅，**后门也用它**）、
  `OneShot`、`Verbose`。
- 判定用的是原版状态位（反编译 `HeroControllerStates`）：上劈 `upAttacking`、下劈 `downAttacking` + `!onGround`、
  横挥 `attacking` + `facingRight`、跳 `jumping` 上升沿；**玩家必须待在判定盒里**（除非勾了 `AnywhereInScene`）。
- ⚠ 六个次数全填 0 = **没密码，这个锁会被忽略**（不会随便动一下就传送）——这是故意的护栏。
- 进度日志：`[HKCS] 密码锁 第2段（下攻击）1/2`、`[HKCS] 密码锁 下一段：左攻击 ×3`、
  `[HKCS] 密码锁打开！… 秒后送往 Town:RestBench`（`Verbose` 会记录每一次输入）。

### 9.3 后门 / 保险出口（防止玩家卡在迷宫里）
**不用新组件**：在迷宫（或迷宫任意一间房）里**再摆一个 `PatchComboLock`**，配置成：
- `AnywhereInScene` = **true**（在**这个场景任何地方**做完这组动作就算数，不限区域）；
- 密码填一组**和正式出口不同**的组合；
- `TargetScene` / `TargetBench` = `Town` / `RestBench`（德特茅斯长椅，默认值就是）；
- `OneShot` = false（可以反复用）。
表现和正常出迷宫完全一样（黑屏 → 在德特茅斯椅子上醒来，不掉影子不掉钱）。

---

## 10. 相关文件索引

| 文件 | 作用 |
|---|---|
| `FinalMod/Consts/RoomNames.cs` | **房间表**（Scene / Width / Height / 门名）+ 区域名键名 + 原版门名 |
| `FinalMod/CustomSceneMod.cs` | 主类：预载清单 `GetPreloadNames()`、原版场景 → 房间的路由 `SceneChanger.ResolveRedirect`、区域名文案 |
| `FinalMod/SceneChanger.cs` | 场景切换钩子、尺寸钩子、相机兜底、门工具函数 |
| `FinalMod/TileMapFix.cs` | 自动补 `tk2dTileMap`（相机 NRE 的解药）+ 地形实测日志 |
| `FinalMod/Patchers/PatchBench.cs` | 长椅（存档点）克隆 + 高度对齐 |
| `FinalMod/Patchers/PatchEnemy.cs` | 小怪克隆 + 自动落地 + 唯一击杀 id |
| `FinalMod/Patchers/PatchGhost.cs` | 可梦钉抽的灵魂怪（迷宫入口）|
| `FinalMod/Patchers/PatchComboLock.cs` | 密码锁出口区域 |
| `FinalMod/Patchers/PatchAreaTitleController.cs` | 区域名控制器 |
| `MonoBehaviours/*.cs` | Unity 侧**空壳**（字段必须和 FinalMod 里逐字一致）|
| `UnityProject/HKModCustomScene/Assets/Editor/*.cs` | 编辑器工具：生成 `__Initializer`、打包、预览、图标、Create Collision |
| `tools/打包测试.cmd`、`tools/更新壳工程.cmd` | 一键流程 |
| `教学-从零理解.md`、`教学-阶段三-详细操作.md` | 从零/阶段三的逐步教学 |
| `handoff.md` | 进展与踩坑记录（每个结论都有实测证据）|
