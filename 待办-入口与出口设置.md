# 你还要做的步骤（2026-09-26 最新版：文本全部可配置 + 出口怪自愈）

> **出口** = 摆在迷宫里的另一只**灵魂怪**：跟它交互（按上/下，像坐椅子那样）→ 弹出**你配的文本** →
> **退出交互的瞬间** → 死亡式传送 → 在**德特茅斯椅子上**醒来。
> **入口** = 德特茅斯掘墓者右边那只，代码写死，**不用摆**：交互弹①、**梦钉**弹②，然后死亡传送进迷宫椅子。
> **所有给玩家看的字都在 `DialogueConfig.json` 里**（改文本**不用重新打包**，见 §0）。
> 密码锁（`PatchComboLock`）只留给"想再摆一个后门"的场合，全局后门已经自动生效。

---

## 0. 只做一次的前提

- [ ] **`tools\更新壳工程.cmd`** —— 本次（0.43）**没有改任何摆放字段**，所以**不用**重跑；
      只有你之前一直没更新过壳 dll（Unity 里选不到 `PatchGhost` 或字段对不上）才需要跑一次。
- [ ] 迷宫椅子 `PatchBench.BenchName` = **`HKCS_Bench`**（**入口**灵魂怪的目标写死在代码里就是它）
- [ ] **`DialogueConfig.json`**：`tools\打包测试.cmd` 会**只在缺失时**把仓库根目录那份放到
      `Managed\Mods\CustomScene\` 里；游戏启动时如果还是没有，mod 自己会按出厂模板写一份。
      **改文本 = 直接编辑 mod 目录里那个 json → 存盘 → 重启游戏**（不用重新打包，也不要指望热更新）。

---

## A. 入口灵魂怪：**不用摆** ✅（代码写死）

- 出现在**德特茅斯掘墓者右边 4 格**（`215.64, 8.33`），**克隆原版掘墓者外观**；
- **按上/下交互** → 显示 ①`entryInteract`；
- **梦钉抽它** → 显示梦语 ②`entryDream` → 死亡动画 → 在迷宫 `HKCS_Bench` 椅子上醒来；
- 井口↔井底已恢复原版（跳井照原版下十字路），所以进迷宫只有这一条路。

---

## B. 出口灵魂怪：**你要摆的就是这一只**（在迷宫里）

### B1. 摆法
- [ ] 迷宫里出口位置 `Create Empty`（如 `ExitGhost`）→ `Add Component` → **`PatchGhost`**
- [ ] 按下表填（**关键：`SpawnInVanillaScene` 不勾 = 就地生成 = 出口模式**）

| 字段 | 填什么 | 说明 |
|---|---|---|
| `SpawnInVanillaScene` | ❌ **不勾** | 勾了才是"注入原版场景"（那是入口的用法，入口已写死） |
| `UseVanillaLook` | ✅ **勾上** | 克隆原版掘墓者 → **这样才能跟它对话**（贴图自建的幽灵不会说话） |
| `DialogueText` | 可以留空 | 文本现在由 `DialogueConfig.json` 的 ③`exitInteract` 决定；**只有把 ③ 写成 `""`** 时才会用这里填的（想给每只怪写不同文本时这么用） |
| `GhostSprite` | 留空 | 只有 `UseVanillaLook` 不勾时才用 |
| `TargetScene` | `Town` | 传送目的地（德特茅斯） |
| `TargetBench` | `RestBench` | 德特茅斯那把长椅（实测名，别改） |
| `Delay` | `1.5` | 对话结束后等几秒再传送 |
| `GiveSoul` | 不勾 | 梦钉抽它给不给灵魂（出口用不上） |

**出口怪的行为（2026-09-26 按用户要求定的）**

| 你对它做什么 | 会发生什么 |
|---|---|
| **走到跟前按上/下（交互）** | 弹 ③`exitInteract` 那句话（默认「你的勇敢征服了我，地狱不是你该来的地方，现在我送你回去。」） |
| **对话收起（退出交互）** | **不死亡**地送回德特茅斯长椅：黑屏 → 直接出现在椅子上（**没有死亡动画、不掉影子、不掉钱**） |
| **用梦钉抽它** | **没有任何反应**（不弹字、不传送、不给灵魂）—— `ghost_npc_dreamnail` FSM 被关 + `convoAmount=0` + 钩子直接吞掉这一下，三重保险 |

- [ ] ⚠ **摆放点上不要挂 SpriteRenderer / 精灵当图标**：那个物体在游戏里也是真的，
      图标会被**真画出来**，而它的贴图/材质不在我们的 AssetBundle 里 ⇒ **渲染成一块洋红方块**，
      正好盖在生成的灵魂怪身上（2026-09-26 的"只有一个坏图标、怪没出来"就是这么来的）。
      现在运行时**会自动拆掉**摆放点上的美术件（日志：`… 是「摆放点」——已拆掉它身上 N 个美术件`），
      但你自己在编辑器里把那个 SpriteRenderer 删掉更干净。**要认位置就看 Gizmo / 场景里那个空物体。**

### B2. 验收
- [ ] `tools\打包测试.cmd` → 进迷宫 → 走到出口灵魂怪跟前 → **按上/下交互** → 应显示 ③ 那句话 →
      **对话一收起** → **黑屏后在德特茅斯长椅上醒来**（**不是**死亡、影子还在原地不动、钱不掉）。
- [ ] 顺带验：**用梦钉抽它 → 什么都不该发生**（骑士不受伤、不传送、不弹字）。
- [ ] 期望日志：
```
[HKCS][对话配置] (已读取) …\Mods\CustomScene\DialogueConfig.json
[HKCS][对话配置]   · ① 入口怪交互：「…」 / ② 入口怪梦语：「…」 / ③ 出口怪交互：「…」
[HKCS] ExitGhost 的交互文本：「…」（来源：DialogueConfig.json 的 exitInteract）
[HKCS] 灵魂怪模式：出口（对话结束→传送）
[HKCS] 强制现形：视觉件本来就是好的；关掉的 FSM：ghost_npc_dreamnail；保留的 FSM：Appear、npc_control、Conversation Control、FSM、FSM、fader、Bob、ghost_npc_death
[HKCS] 出口灵魂怪目标：Town:RestBench（**交互对话结束就传送，不杀死骑士**，延迟 1.5 秒；梦钉抽它没有任何反应）
[HKCS][看门人] 已就位，看着 HKCS_GHOST_ExitGhost
[HKCS] 放了灵魂怪 HKCS_Ghost_HKCS_GHOST_ExitGhost @ (…)（外观=克隆原版掘墓者，…，生成后 active=True，所在场景=HKCS_Room01）
… 交互后 …
[HKCS] 交互文本替换：GRAVEDIGGER_TALK → HKCS_GHOST_ExitGhost（sheet=Ghosts）
[HKCS] 交互文本命中：key=HKCS_GHOST_ExitGhost sheet=Ghosts → 「你的勇敢征服了我，…」
[HKCS] 出口灵魂怪开始对话，等对话结束后传送
[HKCS] 交互结束 → 触发出口传送（不杀死骑士）
[HKCS] 出口灵魂怪：交互结束 → **不死亡**传送（1.5 秒后 → Town:RestBench，黑屏后在长椅上醒来）
[HKCS] 走原版「在长椅上醒来」流程（ReadyForRespawn），不杀骑士、不掉影子
… 梦钉抽它时 …
[HKCS] 梦钉命中**出口**灵魂怪 → 按用户要求：**毫无反应**（不放行、不弹字、不传送）
```
- [ ] **怎么判断"这回真的出来了"**：`[看门人]` 那行里
      `激活=True 在层级里=True`、`Character Sprite … 可见=True`、**`贴图=` 后面是一个名字不是 `null`**。
      · 如果 `激活=False` ⇒ 有原版逻辑在关它（看门人会**自己打开**并打红字，把红字发我）；
      · 如果 `贴图=**贴图=null ⇒ 画出来就是洋红块**` ⇒ 是美术资产没进包，把那行发我；
      · 如果**完全没有** `[看门人]` 行 ⇒ 连看门人都没活下来，那就把整个 ModLog 发我。
- [ ] **没反应怎么查**：
  - 没有 `交互文本替换：…` ⇒ 按上/下时那只怪没走对话系统（确认 `UseVanillaLook` 勾了）；把
    `原版灵魂怪 FSM 字符串：…` 那几行发我，我精确锁定它的对话键；
  - 有 `开始对话` 但没有 `交互结束` ⇒ 对话框收起的路径我钩的是 `DialogueBox.HideText`，把日志发我换钩点；
  - 有 `交互结束` 但没换场景 ⇒ 目标椅子/场景名写错（`Town` / `RestBench`）。

---

## C. 后门：**不用摆** ✅（密码锁现在只用于"想额外摆一个"的场合）

- **全局后门**（自动，任意本 Mod 房间内）：`上攻击 ×2 → 左攻击 ×1 → 右攻击 ×1 → 左攻击 ×1 → 右攻击 ×1 → 左跳 ×1 → 右跳 ×1` → 德特茅斯椅子。
- 想改密码：`FinalMod/GlobalBackdoor.cs` 顶部 `StepKinds[] / StepDirs[]`（**数组里每一项就是一次动作**，
  要按两下就写两项；顺序不对只忽略、不清零）。
- 如果你想在某处再摆一个"密码锁后门"（可选）：空物体 + `PatchComboLock`，
  `Sequence` 数组按顺序填（例如 `AttackUp, AttackUp, AttackDown, AttackDown, AttackLeft, AttackRight, AttackLeft, AttackRight`），
  `AnywhereInScene` 勾上 = 整场景任何地方做完都算。

---

## D. 完整流程（照做）

```
1) tools\更新壳工程.cmd
2) Unity 里：迷宫中摆 ExitGhost（B1 的字段表，别挂 Sprite）；入口不用摆；不需要光效
3) tools\打包测试.cmd
4) 验：入口（德特茅斯灵魂怪 → 交互看①/梦钉看② → 迷宫椅子）
     出口（迷宫灵魂怪 → 交互看③ → 对话结束 → **不死亡**传送回德特茅斯椅子；梦钉抽它无反应）
     后门（房间里做 §C 那串动作 → 德特茅斯椅子）
5) 想改字：编辑 <游戏>\hollow_knight_data\Managed\Mods\CustomScene\DialogueConfig.json → 重启游戏
```

---

## E. 最容易踩的 6 个点

1. **出口灵魂怪必须勾 `UseVanillaLook`**（克隆原版才有对话）；`SpawnInVanillaScene` 必须**不勾**。
2. **摆放点上别挂 Sprite 当图标** —— 游戏里会渲染成洋红方块（运行时现在会自动拆，但编辑器里删掉最好）。
3. **出口怪不再是死亡传送**：对话结束 → 黑屏 → 直接坐回德特茅斯长椅（**不掉影子、不掉钱、不播死亡动画**）；
   梦钉抽它**没有任何反应**。（**入口**怪仍然是死亡式过渡：梦钉 → 梦语 → 在迷宫椅子上醒来。）
4. 文本一律走 `DialogueConfig.json`：①②③ 三项；**留空 = 用出厂默认**，只有 ③ 写 `""` 才是"用组件上的 DialogueText"。
   改文本**不用重新打包**（重启游戏即可）；写成坏 JSON 也不会崩，日志会告诉你第几行第几列错在哪。
   ⚠ 文件必须是 **UTF-8**（记事本另存时选 UTF-8；**不要**存成 ANSI/GBK，否则中文会变乱码）。
5. `TargetBench` / 入口目标椅子必须是 `HKCS_Bench`（入口那只写死在代码里）。
6. 场景资源 AssetBundle 名必须 `hkcs_scenes`，否则日志报 `场景包里找不到房间 XXX`。
