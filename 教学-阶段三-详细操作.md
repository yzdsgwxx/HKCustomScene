# 阶段三 · 一间房：操作手册（第 11～20 步）

> 这份文件是 `教学-从零理解.md`「阶段三」那张表格的**展开版**。
> 表格只给了索引（一行一句），本文把每一步写成：**目标 → 点哪里 → 字段填什么 → 怎么验收 → 出错怎么办**。
>
> 全文标注约定：
> - ✅ **已在本机实测/反编译核对过**的事实（不是"据说"）
> - 🔧 本仓库自带的现成工具（不用手搓）
> - ⚠️ 坑 / 未完成的缺口
>
> 依据：官方教程 [ModdingDocs · Creating custom Scenes with Unity](https://prashantmohta.github.io/ModdingDocs/new-scene.html)
> ＋本仓库代码 `FinalMod/`、`MonoBehaviours/`、`UnityProject/Assets/Editor/`
> ＋ SFCore v1.5.16.2 源码 ＋ **游戏本体 `Assembly-CSharp.dll` 反编译**（本机已装游戏，见 §0.2）。

---

## 目录

| 章节 | 内容 |
|---|---|
| [§0](#0-开工前必读) | 开工前必读：五条硬规则、已就绪/还缺什么、动手前检查 |
| [§1](#1-步骤总览) | 十步总览表 |
| [§2](#2-逐步操作) | **逐步操作 11～20**（正文） |
| [§3](#3-原理与术语卡住再看) | 原理与术语（卡住再看）：两个 dll、空物体、Layer/Tag、AssetBundle |
| [§4](#4-待补--已知缺口) | 待补 / 已知缺口（`PatchTileMap`、64×32 vs 30×17） |
| [§5](#5-修复记录本会话已改的代码) | 修复记录：本会话已改的代码 |
| [§6](#6-排查总表) | 排查总表（症状 → 原因 → 处理） |

---

## 0. 开工前（必读）

### 0.1 五条硬规则

1. **摆场景的是"真工程"**：`hkmod-custom-scene\UnityProject\HKModCustomScene\`。
   外层那个 `UnityProject\`（里面也有 `Assets\`）是**骨架/拷贝源**，往它里面摆场景等于白干。
2. **名字必须逐字一致**。整个流程里有 5 个关键名字，谁在引用它们：

   | 名字 | 谁在用它 |
   |---|---|
   | 场景名 `HKCS_Room01` | `.unity` 文件名、`FinalMod/Consts/RoomNames.cs` 的 `Rooms.Room01`、`SceneChanger.ValidateRooms()` 的运行时比对 |
   | AssetBundle 名 `hkcs_scenes` | 第 16 步设在场景上、第 17 步打出同名文件、csproj 的 `LogicalName` |
   | `_Managers` | `Assets/Editor/CreateInitializer.cs` 用 `GameObject.Find` 找它 |
   | `__Initializer` | 同上（2 个下划线） |
   | `left1` / `right1` | `RoomNames.cs` 的 `fromPrev` / `toNext`、`TransitionPoint.entryPoint` 按物体名查找 |

3. **"挂 X" = Inspector → Add Component → 搜 X**。Unity 里没有任何别的意思。
4. **场景里所有物体的 Z 坐标 = 0**（空洞骑士是 XY 平面的 2D 游戏）。
5. **Unity 加载场景时自动调用场景里每个 MonoBehaviour 的 `Awake()`** —— 所以"进房间时执行的代码"必须挂在场景里某个物体上。

### 0.2 现在已就绪 / 还缺什么

| 项 | 状态 | 位置 / 说明 |
|---|---|---|
| Unity 2020.2.2f1 | ✅ | `D:\UnityEngine\2020.2.2f1` |
| 真工程 | ✅ | `UnityProject\HKModCustomScene\` |
| `TagManager.asset`（HK 的 tags/layers/sorting layers） | ✅ 已覆盖 | Layer 8 = `Terrain` |
| 壳 dll `HKCustomSceneMod.dll` | ✅ 已编译并拷入 | `Assets\Assemblies\`（4,608 B） |
| `SFCore.dll`（由 `SFCoreUnity.dll` 改名） | ✅ 已下载安装 | `Assets\Assemblies\`（14,336 B） |
| 纯黑贴图 `black.png` | ✅ 已生成 | `Assets\Materials\black.png`（第 13 步要用） |
| **游戏本体** | ✅ **已装** | `D:\APP\steam.exe\steamapps\common\Hollow Knight\`（非 Steam 默认路径） |
| Modding API **v77** | ✅ 已装 | `Managed\` 里的文件与 `moddingapi.v77.windows\` 哈希逐字节相同 |
| SFCore 依赖 mod | ✅ 已装 | `Managed\Mods\SFCore\SFCore.dll` |
| FinalMod 能不能编译 | ✅ **能**（0 警告 0 错误，本会话修完） | `FinalMod\HKCustomSceneMod.csproj` 的 `HollowKnightRefs` 已指向真实路径 |
| `PatchTileMap` | ⚠️ **没有**（唯一挡在第 20 步前的东西） | 见 [§4.1](#41-patchtilemap唯一挡路的缺口) |

### 0.3 动手前的三个检查

1. **Console 没有红字**：Unity 底部 Console → `Clear` → 再点回 Unity 触发刷新。红字状态下 Unity 用的是旧 dll、组件列表也是旧的。
2. **`Assets\Assemblies\` 里两个 dll 都在**（`HKCustomSceneMod.dll` + `SFCore.dll`）。
3. **`Assets\Materials\black.png` 在**（第 13 步要拖）。

---

## 1. 步骤总览

| # | 做什么 | 产物 / 结果 | 在哪做 |
|---|---|---|---|
| 11 | 新建场景 `HKCS_Room01` | `Assets/Scenes/HKCS_Room01.unity` | Unity |
| 12 | 摆地形 | 地形物体 + `PolygonCollider2D` + Layer=Terrain | Unity |
| 13 | 地形挂 `SceneMapPatcher` | 地形在游戏里"进地图但不被相机画" | Unity |
| 14 | 建 `_Managers` + `__Initializer` | 一个空物体挂 3 个 Patch 组件 | Unity（🔧 有菜单一键做） |
| 15 | 建出入口 | `left1` / `right1` + `TransitionPoint` | Unity |
| 16 | 给场景设 AssetBundle 名 | 场景被标记为 `hkcs_scenes` | Unity |
| 17 | 打包（点菜单） | `Assets/AssetBundles/hkcs_scenes` | Unity |
| 18 | 把包拷进 FinalMod | `FinalMod/Resources/hkcs_scenes` | 资源管理器 |
| 19 | 编译 FinalMod | `Managed/Mods/HKCustomSceneMod/HKCustomSceneMod.dll` | 命令行 |
| 20 | 进游戏验证 | 从德特茅斯水井下去能进新房间 | 游戏 |

依赖关系（箭头 = "先有左边才能做右边"）：

```
11 场景 ──► 12 地形 ──► 13 SceneMapPatcher
                └────► 14 __Initializer        （14 不依赖 12/13，只依赖两个 dll）
15 门 ──────────┘
16 AssetBundle 名 ──► 17 打包 ──► 18 拷进 Resources ──► 19 编译 ──► 20 进游戏
```

> 11～18 全部在 Unity / 文件管理器里完成，**19、20 需要游戏**（本机已具备，但见 §4.1 的缺口）。

---

## 2. 逐步操作

## 第 11 步 · 新建场景 `HKCS_Room01`

**目标**：有一个叫 `HKCS_Room01` 的空场景，作为第一间房。

**为什么名字要命**：这个名字同时出现在三处，必须字字相同 ——
① `.unity` 文件名；② `FinalMod/Consts/RoomNames.cs` 里 `Rooms.Room01 = "HKCS_" + "Room01"`；
③ 运行时 `SceneChanger.ValidateRooms()` 拿场景包里的 `assets/scenes/hkcs_room01.unity` 去比对，
对不上就在日志里报 `[HKCS] 场景包里找不到房间 HKCS_Room01`。

**操作**

1. Project 窗口 → `Assets/Scenes/` 右键 → **Create → Scene**。（**别**拿模板自带的 `SampleScene` 改名，它里面有模板的相机和灯光。）
2. `Ctrl+S` → 保存为 `Assets/Scenes/HKCS_Room01.unity`。**文件名就是场景名**。
3. 新场景自带 `Main Camera` + `Directional Light`：**删掉这两个**。
   相机和灯光由游戏提供并跨场景保留，留在包里只会和游戏相机打架。
4. **不要**把它加进 `File → Build Settings`：AssetBundle 是按包里的名字加载的，加进去没用。

**验收**：Project 窗口有 `Assets/Scenes/HKCS_Room01.unity`；Hierarchy 里基本是空的。

**出错怎么办**

| 现象 | 处理 |
|---|---|
| 场景名和 `Rooms.Room01` 不一致 | 要么改文件名，要么改 `RoomNames.cs`（**改一处就要改另一处**） |

---

## 第 12 步 · 摆地形

**目标**：房间有"能站的地面 + 能撞的墙"，并且游戏认得它是地形。

**先认识这块 mesh**：`TutorialScene.obj` 只有 12 个顶点、2 个面，翻译过来是：

```
地板：  30 宽 × 2 厚（y = 0..2），横跨 x = -30..0
墙+顶： 厚 2 的一圈（y = 6..17），中间挖空
可走空间：y = 2..15；下半部分 x = -30..0，上半部分 x = -28..-2
```

`.obj` 是纯文本，想改造地形直接记事本编辑再让 Unity 重导。

**操作**

1. Project 窗口把 `Assets/Meshes/TutorialScene.obj` 拖进 Hierarchy。
2. 得到的物体改名 `Terrain`（名字随意，只为好找），Transform 保持 `(0,0,0)`、缩放 `(1,1,1)`、**Z = 0**。

> ⚠️ **实测大坑（2026-09-25 首次进游戏踩中）**：把 `.obj` 拖进场景，Unity 生成的是**模型 prefab 实例**，
> 而 `Meshes/TutorialScene.obj.meta` 里是 **`addColliders: 0`** —— 这个实例**只有 MeshFilter + MeshRenderer，
> 没有任何碰撞体**；而且**模型 prefab 不携带 Layer**，实例默认停在 `Default(0)`。
> 后果：骑士进来直接穿过地面往下掉（画面就是黑背景直坠）。
> 所以下面第 3、4 步**一步都不能省**，别指望"拖进来就有地面"。
> 也**不要**去勾模型导入设置里的 `Generate Colliders` —— 那生成的是 3D `MeshCollider`，空洞骑士的 2D 物理认不出。

3. 选中 `Terrain` → Inspector 左上 **Layer → `Terrain`**（就是索引 8）。
   选不到 `Terrain` ⇒ 阶段二的 `TagManager.asset` 没覆盖成功，回阶段二。
4. 加碰撞体：`MeshFilter` 组件右上角 `⋮` → **Create Collision** 🔧
   （`Assets/Editor/MeshCollisionCreator.cs` 提供，会自动加 `PolygonCollider2D` 并按网格外轮廓生成路径）。
   也可以 `Add Component → PolygonCollider2D` 自己描，或 `EdgeCollider2D` 手画线段。
   **不要**用 3D 的 `MeshCollider`：空洞骑士走 Physics2D，认不出 3D 碰撞体 → 骑士穿地。
5. 给 `MeshRenderer` 一个材质（不然编辑器里看不清）：
   `Assets/Materials/` 右键 → **Create → Material** → 命名 `Terrain.mat` → Shader 选 `Unlit/Color` →
   拖到 `Terrain → MeshRenderer → Materials`。
   这个材质只是给你看的，游戏里会被第 13 步的 `SceneMapPatcher` 换掉。

**层级怎么摆** —— 两条路，**推荐 B**：

| 方案 | 层级 | 说明 |
|---|---|---|
| A（最省事） | `Terrain`（MeshFilter + MeshRenderer + PolygonCollider2D + SceneMapPatcher） | 只有一块 mesh 时够用；**但第 20 步会因为找不到 tilemap 而失败** |
| **B（推荐）** | 见下 | 官方布局，为 `PatchTileMap` 预留好位置，以后不返工 |

```
TileMap                  ← Tag 必须设成 TileMap；将来挂 PatchTileMap（运行时生成 tk2dTileMap）
TileMap Render Data      ← 空物体，被 TileMap 引用
  └ Scenemap             ← Layer = Terrain；挂 SceneMapPatcher
      └ Chunk 0 0        ← Layer = Terrain；pos (0, 0, -0.1)；MeshFilter=TutorialScene + MeshRenderer + PolygonCollider2D
```

> 为什么 B 里 `TileMap` 必须带 Tag：✅ 反编译确认游戏是这样找 tilemap 的 ——
> `GameManager.GetTileMap(go)` 只在 **`go.CompareTag("TileMap")` 成立时**返回 `go.GetComponent<tk2dTileMap>()`，
> 否则返回 null。也就是说：**光叫 `TileMap` 不行，Tag 也得是 `TileMap`**（Tag 来自阶段二覆盖的 `TagManager.asset`）。

**验收**：`Terrain`（或 B 方案里的 `Chunk 0 0`）`Layer = Terrain`、有 `PolygonCollider2D` 且 `Points` 非空、
Scene 视图能看到面片。

---

## 第 13 步 · 地形挂 `SceneMapPatcher` + 给一张纯黑贴图

**目标**：让地形在游戏里"参与小地图，但不被相机直接画出来"——原版房间的地形就是这个状态。

### 13.1 操作

1. 选中**地形所在的物体**：
   - 方案 A → 选 `Terrain`；
   - 方案 B → 选 `Scenemap`（`SceneMapPatcher` 会作用到它自己和所有子物体）。
2. Inspector → **Add Component** → 搜 `SceneMapPatcher` → 点它。
3. `Tex` 槽：把 `Assets/Materials/black.png` 拖进去。

### 13.2 ⚠️ 会出现两个同名组件，选哪个

| 来自 | 全名 | 说明 |
|---|---|---|
| `SFCore.dll` | `SFCore.MonoBehaviours.SceneMapPatcher` | ✅ **用这个**（官方教程用的，SFCore 是必装依赖，运行期一定在） |
| `HKCustomSceneMod.dll`（壳 dll） | `HKCustomSceneMod.Patchers.SceneMapPatcher` | 本仓库自己实现的，功能等价；**别两个混用** |

分辨方法：Add Component 列表里选中时会显示完整类型名；挂上去后 Inspector 的组件标题旁也能看出脚本归属。

### 13.3 `Tex` 为什么是"一张纯黑贴图"、怎么弄到手

**字段类型是 `Texture`（不是 `Sprite`）**。它的作用：运行时把地形的 shader 换成游戏里的
`tk2d/BlendVertexColor` 并贴上这张图，地形就"不显示但参与地图"。

**获取方式（三选一）**：

1. ✅ **我已经生成好了**：`Assets/Materials/black.png`（8×8、A255 R0G0 B0、143 字节）。
   Unity 刷新后直接从 Project 窗口拖到 `Tex` 槽。
2. 自己画：画图/PS 新建 8×8 画布 → 填纯黑 → 导出 PNG → 拖进 `Assets/Materials/`。
   Unity 默认按 `Texture Type = Default` 导入，正好能拖进 `Texture` 槽。
3. 命令行（和我的做法一样）：
   ```powershell
   Add-Type -AssemblyName System.Drawing
   $bmp = New-Object System.Drawing.Bitmap 8,8
   $g = [System.Drawing.Graphics]::FromImage($bmp); $g.Clear([System.Drawing.Color]::Black); $g.Dispose()
   $bmp.Save("D:\HKModding\hkmod-custom-scene\UnityProject\HKModCustomScene\Assets\Materials\black.png",
             [System.Drawing.Imaging.ImageFormat]::Png)
   ```

⚠️ **Unity 没有"凭空新建贴图"的菜单**：`Assets → Create` 里找不到 Texture，贴图必须从外部文件导入。

### 13.4 运行时它干什么

```csharp
// SFCore.MonoBehaviours.SceneMapPatcher.Start()
foreach (var cMr in gameObject.GetComponentsInChildren<MeshRenderer>(false))
{
    cMr.material.shader = Shader.Find("tk2d/BlendVertexColor");
    cMr.material.SetTexture(Shader.PropertyToID("_MainTex"), tex);
}
```

⚠️ `tk2d/BlendVertexColor` 是**游戏里**的 shader，Unity 编辑器工程里没有。
所以在 Unity 里按 Play 会报"找不到 shader"——**这是正常的**，HK Mod 的场景一律进游戏测，不做编辑器 Play 测试。

**验收**：目标物体上有 `SceneMapPatcher` 且 `Tex` 有值；`MeshRenderer` 有材质；`Layer = Terrain`。

---

## 第 14 步 · 建 `_Managers` + `__Initializer`

**目标**：给"进房间时要执行的代码"一个落脚点，给"运行时克隆出来的管理器"一个父物体。

### 14.1 一句话结论

**建两个空 GameObject**，名字分别叫 `_Managers`（1 个下划线）和 `__Initializer`（2 个下划线），
Transform 都是 `(0,0,0)`；**只在 `__Initializer` 上挂 3 个组件，`_Managers` 上什么都不挂。**

### 14.2 推荐做法：用现成菜单 🔧

菜单 **`Tools → HKCS → 生成 __Initializer`**（代码 `Assets/Editor/CreateInitializer.cs`）。它会：

1. 没有就建 `_Managers`；
2. 没有就建 `__Initializer`；
3. 给 `__Initializer` 挂（缺哪个补哪个，已存在则跳过）：
   - `HKCustomSceneMod.Patchers.PatchAreaTitleController` → 自动填 `Sub Area = false`、
     `AreaEvent = HKCustomSceneMod_AreaTitle`、`VisitedBool = HKCustomSceneMod_VisitedArea`；
   - `HKCustomSceneMod.Patchers.PatchPlayMakerManager` → 自动把 `ManagerTransform` 指向 `_Managers`；
   - `SFCore.MonoBehaviours.SceneManagerPatcher` → 默认值。
4. Console 打一行 `[HKCS] __Initializer 生成完毕，新挂上 N 个组件。`
   —— `N < 3` 说明有类型没找到，紧跟的 Warning 会告诉你缺的是壳 dll 还是 `SFCore.dll`。

做完检查一眼，`Ctrl+S` 存场景。

### 14.3 手动做法（菜单不出现时用）

1. Hierarchy 右键 → **Create Empty** → 命名 `_Managers`，Transform `(0,0,0)`。
2. Hierarchy 右键 → **Create Empty** → 命名 `__Initializer`，Transform `(0,0,0)`。
3. 选中 `__Initializer` → **Add Component**，加这三个：

| 组件 | 必填字段 | 值 |
|---|---|---|
| `PatchAreaTitleController` | `Sub Area` | **取消勾选**（第一间房这样；同区域后续房间勾上并给不同 `Area Event`） |
| | `Area Event` | `HKCustomSceneMod_AreaTitle` |
| | `Visited Bool` | `HKCustomSceneMod_VisitedArea` |
| `PatchPlayMakerManager` | `Manager Transform` | 把 `_Managers` 拖进来 |
| `SceneManagerPatcher`（SFCore） | `Map Zone` | `TOWN` |
| | `Manual Map Trigger` | `false`（`false` 才允许长椅触发地图） |
| | 其余 | 默认即可（`Environment Type`/`Darkness Level`/`No Lantern`…） |

### 14.4 这两个空物体是什么（为什么要有它们）

**它们都是"空 GameObject"**：Unity 里 GameObject 本身只有 Transform，能力全来自挂载的组件。
这两个物体没有网格、没有图、没有碰撞体，Scene 视图里看不见。它们的价值只有两条：
**① 给组件一个落脚点；② 有个名字方便工具找到它。**

- **为什么"挂组件"就能执行代码**：Unity 加载场景时自动调用每个 MonoBehaviour 的 `Awake()`。
  "我要在进房间时干点事" = 把组件挂在场景里某个物体上，总得有个物体来背 → 于是建 `__Initializer`。
- **`__Initializer` 这个名字游戏认不认**：✅ 不认。反编译游戏本体搜字符串 `__Initializer` **出现 0 次**
  （`_Managers`、`_SceneManager` 作为名字同样 0 次：游戏靠**组件和 Tag**找东西，不靠物体名）。
  但**名字要照抄**，因为本仓库的编辑器工具是用 `GameObject.Find("__Initializer")` 找它的。
- **`_Managers` 是干什么的**：空洞骑士的 PlayMaker FSM 需要场景里有个 `PlayMaker Unity 2D` 辅助物体
  （把 2D 物理事件转发给 FSM）。它没有它，跟**名字/文字**有关的 FSM 动作会**静默失效**（区域名、NPC 名可能空白）。
  那东西是**游戏里的 prefab**，Unity 里造不出来 → 只能运行期从预载克隆 → 克隆体需要个爹，**爹就是 `_Managers`**：
  ```csharp
  // FinalMod/Patchers/PatchPlayMakerManager.cs
  GameObject go = UObject.Instantiate(PrefabHolder.PlayMakerUnity2D, ManagerTransform); // ManagerTransform = _Managers
  ```
- **进阶**：`_SceneManager` **不用你建** —— SFCore 的 `SceneManagerPatcher.Awake()` 会自己
  `new GameObject("_SceneManager")` + `AddComponent<SceneManager>()`，填字段、打 Tag、通知 `GameManager`。
  手动放一个只会打架。（`FinalMod/PrefabHolder.cs` 里那个 `SceneManagerPrefab` 因此是死代码，无害。）

**进房间时的执行时序**：

```
场景加载完成
  └─ Unity 对场景里每个 MonoBehaviour 调用 Awake()
       ├─ PatchAreaTitleController.Awake()  克隆预载的 "Area Title Controller" → 灌 FSM 变量 → Destroy(自己)
       ├─ PatchPlayMakerManager.Awake()     克隆预载的 "PlayMaker Unity 2D" → 挂到 _Managers 下
       └─ SceneManagerPatcher.Awake()       造 _SceneManager → 通知 GameManager
```

⚠️ `PatchAreaTitleController.Awake()` 最后会 `Destroy(gameObject)`（**删掉它所在的整个物体**），
所以**绝不能把它挂到地形/管理器等有用的物体上**。三个组件同挂 `__Initializer` 没问题：
`Destroy` 延迟到帧末执行，不会影响另外两个。

**验收**：Console 有 `[HKCS] __Initializer 生成完毕，新挂上 3 个组件`；
`__Initializer` 上三个组件齐全，`PatchPlayMakerManager.ManagerTransform` = `_Managers`。

---

## 第 15 步 · 建出入口（`left1` / `right1`）

**目标**：房间左右各一道门，能进来也能出去。

### 15.1 先看这张路线表（四个方向各归各的门）

一个房间有两个"入口方向"，**落点必须分开**，否则两个方向会挤在同一侧；
另外每个房间门"出去"时指向原版场景的哪个门，也要单独配：

| 方向 | 谁触发 | 落点 / 目标 | 在哪配 |
|---|---|---|---|
| 德特茅斯**跳井** → 房间 | `Town.bot1` | 房间 **`left1`**（西侧） | C#：`VanillaGates.TownGate` + `Rooms[0].FromPrev` |
| 十字路**井底往上爬** → 房间 | `Crossroads_01.top2` | 房间 **`right1`**（东侧） | C#：`VanillaGates.CrossroadsEntry = "right1"` |
| 房间**从左边出去** → 德特茅斯 | 房间 `left1` | `Town` : **`bot1`** | Unity：`left1.TransitionPoint` |
| 房间**从右边出去** → 井底 | 房间 `right1` | `Crossroads_01` : **`top1`** | Unity：`right1.TransitionPoint` |

> ⚠️ **`left1` 的 `Entry Point` 填 `bot1`，不是 `top1`**（2026-09-25 实测踩过）：
> 填 `top1` 时从房间左边出来会**出现在德特茅斯上空然后掉下来**。
> 官方教程此处也是 `bot1` —— `bot1` 就是德特茅斯那口井本身，回到那里＝回到井口。

**为什么名字必须是 `left1` / `right1`**：`RoomNames.cs` 里 `Room01` 的 `fromPrev = "left1"`；
而 `entryPoint` 是**按"目标场景里叫这个名字的物体"去找的** —— 名字错一个字就落在默认位置（通常在虚空里）。

**操作**

1. Hierarchy 右键 → **Create Empty** → 命名 `_Transition Gates`（纯整理用，`(0,0,0)`）。
2. 在它下面建 **`left1`**，世界坐标 `(-29.5, 4, 0)`（地板左边缘内侧），Add Component：
   - `BoxCollider2D`：勾 **Is Trigger**，`Size` = `1 × 4`
   - `TransitionPoint`：勾 `Always Enter Left`；`Target Scene` = `Town`；`Entry Point` = **`bot1`**
3. 建 **`right1`**，世界坐标 `(-0.5, 4, 0)`（地板右边缘内侧），Add Component：
   - `BoxCollider2D`：勾 **Is Trigger**，`Size` = `1 × 4`
   - `TransitionPoint`：勾 `Always Enter Right`；`Target Scene` = `Crossroads_01`；`Entry Point` = `top1`
     （官方教程特意注明用 `top1` 而不是 `top2`：`top1` 会让骑士从低处掉进来）
4. 每道门下面各建一个子物体 **`Hazard Respawn Marker`**：
   - Add Component → `HazardRespawnMarker`（`Respawn Facing Right` 按朝向勾）
   - 位置拖到**门内侧的地板面上方**（如 `left1` 的放到世界 `(-27, 3, 0)`；Scene 视图里用鼠标拖最直观）
   - 回到门的 `TransitionPoint` → `Respawn Marker` → 把该子物体拖进去
   - **为什么必须有**：没有它，玩家在这间房摔死后会重生在很奇怪的地方

**位置不敏感，但有一条硬要求**：`BoxCollider2D` 是触发区，骑士走进去就触发；
**别把触发区埋进实心墙里**（骑士撞墙就停住，永远进不去）。先在 Scene 视图核对触发框与地形边缘对齐。

### 15.2 ⚠ 落点规则：HK 按「门名」决定骑士从哪进来（反编译 `HeroController.EnterScene` 得到）

`TransitionPoint.GetGatePosition()` 就是 `name.Contains("top"/"right"/"left"/"bot"/"door")`，
然后落点按这个结果算（`gate` = **目标场景里那道门**，不是出发的门）：

| 门名含 | 落点 | 效果 |
|---|---|---|
| `left` | `x = gate.x + entryOffset.x + 2`，y 贴地 | 骑士出现在门**右侧 2 格**、面朝右 |
| `right` | `x = gate.x + entryOffset.x − 2`，y 贴地 | 骑士出现在门**左侧 2 格**、面朝左 |
| `bot` | `x = gate.x + entryOffset.x`，`y = gate.y + entryOffset.y + 3` | 从门**上方 3 格**落下来 |
| `top` | `x/y = gate.x/y + entryOffset`（**没有任何额外偏移**） | 骑士**正好落在门的位置**上 ⚠ |
| `door` | 门位置贴地 | 原地站着 |

**由此得到两条必须遵守的规则**：

1. **`BoxCollider2D` 的 `Offset` 必须是 `(0,0)`**，触发框要以门为中心。
   因为骑士会被放在"门 ±2 格"处——**如果框被拖偏了，落点就会正好落进框里 → 落地瞬间被自己这道门弹回去**
   （2026-09-25 实测：`right1` 在 x=31.52，落点 x=29.52，而框因为 `Offset=(−2.54,−0.12)` 实际覆盖
   `x∈[28.00, 29.97]` → 落点在框内 → 弹回井底，来回循环）。
   把它改回 `Offset=(0,0)`、`Size=(1,4)` 后框变成 `x∈[31.02,32.02]`，落点 29.52 在框外 1.5 格 ✓。
   （`left1` 之所以一直没事：它在 x=3.6，落点 x=5.6，而框在 `x∈[0.08,2.05]` —— 本来就在框外。）
2. **`top*` 名字的门最危险**：落点就是门自身位置，所以那道门**不能带以自己为中心的触发框**，
   否则一进来就被弹走。要么不给它框（只当落点用），要么把框挪开，要么给 `entryOffset` 偏移
   （`entryOffset` 是 public 但 `[HideInInspector]`，只能在代码里设）。


**验收**：Hierarchy 里两个物体名精确为 `left1` / `right1`；每个都有
`BoxCollider2D(Is Trigger)` + `TransitionPoint`（`Respawn Marker` 有值）+ 一个 `HazardRespawnMarker` 子物体。`Ctrl+S`。

---

## 第 16 步 · 给场景设 AssetBundle 名

**目标**：让这个场景"可被打包"，名字叫 `hkcs_scenes`。

**操作**

1. Project 窗口选中 `Assets/Scenes/HKCS_Room01.unity`。
2. Inspector **最底部**的 `AssetBundle` 区块：左边下拉 → **`New...`** → 输入 **`hkcs_scenes`** → 回车。
3. 右边 `Variant` 留空。

**为什么**：`BuildPipeline` 只打包"被指定了 AssetBundle 名"的资源（第 17 步详述）。
同一个名字下的所有资源会打进同一个包 —— 以后多房间就都设成 `hkcs_scenes`。

⚠️ **这个名字必须和 C# 侧对得上**：`FinalMod/SceneChanger.cs` 优先加载逻辑名
`HKCustomSceneMod.Resources.hkcs_scenes`，找不到再退回 `...my_first_assetbundle`。

✅ 我已把 `FinalMod/HKCustomSceneMod.csproj` 改成**两条带 `Exists` 条件的 `EmbeddedResource`**：

```xml
<EmbeddedResource Include="Resources\hkcs_scenes"
                  Condition="Exists('$(MSBuildProjectDirectory)\Resources\hkcs_scenes')">
  <LogicalName>HKCustomSceneMod.Resources.hkcs_scenes</LogicalName>
</EmbeddedResource>
<EmbeddedResource Include="Resources\my_first_assetbundle" Condition="Exists('...')">
  <LogicalName>HKCustomSceneMod.Resources.my_first_assetbundle</LogicalName>
</EmbeddedResource>
```

含义：**第 18 步只要把包按原文件名丢进 `FinalMod/Resources/`，就不用再动 csproj**；
而且包还没打出来时编译也不会因为"找不到资源文件"失败。

**验收**：选中场景，Inspector 底部 `AssetBundle` 显示 `hkcs_scenes`。

---

## 第 17 步 · 打包（点一个菜单，没有别的）

**目标**：把场景 + 依赖打成一个二进制包 `hkcs_scenes`。

### 17.1 "打包脚本"是哪个东西

指这个**编辑器脚本文件**：`Assets/Editor/CreateAssetBundles.cs`（✅ 已在真工程里，35 行）。
它**没有界面，也不需要你"运行脚本"** —— 编译后它自动往 Unity 顶部菜单栏加一个菜单：

```
Build AssetBundles
├─ Build AssetBundles Compressed      ← 点这个
└─ Build AssetBundles Uncompressed
```

来源就是文件里的 `[MenuItem("Build AssetBundles/Build AssetBundles Compressed")]`。
**"跑打包脚本" = 点这个菜单项**。

### 17.2 点下去发生什么

执行的是（路径写死在脚本里）：

```csharp
BuildPipeline.BuildAssetBundles("Assets/AssetBundles",
                                BuildAssetBundleOptions.None,
                                BuildTarget.StandaloneWindows64);
```

1. **扫全工程**，找出所有被指定了 AssetBundle 名的资源（= 第 16 步设的 `hkcs_scenes`）；
2. 把它们**和依赖**（网格、材质、贴图、脚本引用……）打成一个 Unity 二进制包；
3. 输出到 `{Unity 工程}/Assets/AssetBundles/`。

⚠️ **前提是第 16 步做过**：没有 AssetBundle 名 → 打完只有空清单，包里没有你的场景。

### 17.3 产物 4 个，只有 1 个有用

| 文件 | 是什么 | 要拷进 FinalMod 吗 |
|---|---|---|
| `hkcs_scenes` | **你的场景包**（无扩展名二进制） | ✅ **只要它** |
| `hkcs_scenes.manifest` | 文本清单，说明包里有什么 | ❌ |
| `AssetBundles` | 总清单包（与输出目录同名） | ❌ |
| `AssetBundles.manifest` | 总清单文本 | ❌ |

**验收**：`Assets/AssetBundles/hkcs_scenes` 存在且**不是 0 字节**；Console 无红字。

### 17.4 常见现象

| 现象 | 原因 / 处理 |
|---|---|
| 顶部菜单栏**没有** `Build AssetBundles` | Editor 脚本没编译成功（Console 有红字）→ 先清红字；确认 `Assets/Editor/CreateAssetBundles.cs` 在真工程里 |
| 点了菜单报错 | 看 Console 原文；多半是第 16 步名字没设或场景没保存 |
| `hkcs_scenes` 只有几百字节 | 包里没东西 → 场景的 AssetBundle 名没设上 |
| 之后改了场景 | **必须重新点一次打包**，然后**重新编译 FinalMod**（新包要重新嵌进 dll） |
| Compressed 还是 Uncompressed | 初期用 Compressed；Uncompressed 加载更快、包更大，排查问题时可临时用 |

---

## 第 18 步 · 把包拷进 `FinalMod/Resources/`

**目标**：让 AssetBundle 成为 dll 的"内嵌资源"，这样发布只有一个文件。

**操作**

1. 在 `hkmod-custom-scene\FinalMod\` 下新建文件夹 `Resources`（**目前还不存在**）。
2. 把 `Assets\AssetBundles\hkcs_scenes` 拷进去 → `FinalMod\Resources\hkcs_scenes`
   （**保持无扩展名、不要改名**）。
3. csproj 里对应的条件式 `EmbeddedResource` ✅ 已加好（见第 16 步）。
4. 想验证逻辑名对不对，编译后跑：
   ```powershell
   [System.Reflection.Assembly]::LoadFrom("D:\HKModding\hkmod-custom-scene\FinalMod\bin\Release\HKCustomSceneMod.dll").GetManifestResourceNames()
   ```
   应该能看到 `HKCustomSceneMod.Resources.hkcs_scenes`。

**验收**：`FinalMod\Resources\hkcs_scenes` 存在，文件名与 csproj 的 `Include` 完全一致。

---

## 第 19 步 · 编译 FinalMod

```powershell
dotnet build D:\HKModding\hkmod-custom-scene\FinalMod\HKCustomSceneMod.csproj -c Release
```

**产物与安装**

- 产物：`FinalMod\bin\Release\HKCustomSceneMod.dll`（✅ 实测 19,456 B）。
- `CopyToMods` 目标会在 `…\Managed\Mods\HKCustomSceneMod\` **已存在**时自动把 dll 拷进去；
  目录不存在就跳过（不报错）。**要装进游戏，先建好这个目录**（名字要和 dll 同名），再编译一次：

  ```
  D:\APP\steam.exe\steamapps\common\Hollow Knight\hollow_knight_Data\Managed\Mods\HKCustomSceneMod\
  ```

**当前状态**：✅ **已实测编译通过（0 警告 0 错误）**。
csproj 的 `HollowKnightRefs` 已指向本机真实游戏路径
`D:\APP\steam.exe\steamapps\common\Hollow Knight\hollow_knight_Data\Managed`；换机器用
`-p:HollowKnightRefs=...` 覆盖。

**壳 vs 真身**：壳工程（`MonoBehaviours`）只引用 Unity 的 `UnityEngine.*`，不需要游戏本体；
FinalMod 必须引用游戏的 `Assembly-CSharp.dll` 等。

（这一步能编过是因为本会话修了 5 处代码 —— 明细见 [§5](#5-修复记录本会话已改的代码)。）

---

## 第 20 步 · 进游戏验证

**前提**：游戏本体 ✅ + Modding API ✅ + SFCore ✅（本机就绪）+
`HKCustomSceneMod.dll` 已在 `Managed\Mods\HKCustomSceneMod\`（第 19 步建目录后重编）+ **§4.1 的缺口处理完**。

**怎么看**：进游戏按 **`F1`** 打开 ModLog，对照下表：

| 日志 / 现象 | 说明什么 |
|---|---|
| `[HKCS] 场景包已加载，含 N 个场景：…` | 包进去了（第 16～18 步对） |
| **没有** `[HKCS] 场景包里找不到房间 HKCS_Room01` | 场景名和 `Rooms` 表对上了（第 11 步对） |
| `[HKCS] HKCS_Room01 尺寸设为 64x32` | `RefreshTilemapInfo` 钩子生效（尺寸那步对） |
| 从德特茅斯水井（`bot1`）下去能进房间 | 门重定向生效（第 15 步的 `left1` 存在） |
| 房间边缘不卡、相机边界正常 | 尺寸正确 |
| 左上角弹出区域名 | `PatchAreaTitleController` + 语言钩子生效（第 13/14 步对） |

> ⚠️ 在这些日志出现之前，先确认 §4.1 —— **`PatchTileMap` 缺失会让"尺寸设为"那行不出现并抛 NRE**。

---

## 3. 原理与术语（卡住再看）

### 3.1 为什么有两个同名 dll

| | ① 壳工程 `MonoBehaviours/` | ② 真身 `FinalMod/` |
|---|---|---|
| 引用 | 只有 `UnityEngine.*`（没有游戏也能编） | 游戏的 `Assembly-CSharp` / `PlayMaker` / SFCore |
| `Awake()` | 空的 | 真正干活 |
| AssemblyName | **`HKCustomSceneMod`** | **`HKCustomSceneMod`**（必须一样） |
| 产物去向 | Unity 的 `Assets/Assemblies/` | 游戏的 `Managed/Mods/HKCustomSceneMod/` |

Unity 在 `.unity` 里存的是"程序集名 + 命名空间.类名 + 字段值"，运行时按这个三元组去**已加载的 dll** 里找同名类，
于是跑的是真身逻辑、用你在 Unity 里填的值。三条铁律：
① 两个工程 AssemblyName 同名；② 命名空间 + 类名一致；③ 壳工程不许引用 HK 的程序集。

### 3.2 空物体与 Awake

GameObject 只有 Transform；"空物体"就是没有其它组件的物体。
场景加载时 Unity 自动调用场景里每个 MonoBehaviour 的 `Awake()` ——
这就是为什么"进房间执行的代码"必须挂在场景里某个物体上（第 14 步的 `__Initializer`）。

### 3.3 Layer / Tag / Sorting Layer

| | 是什么 | 什么时候会坑你 |
|---|---|---|
| **Layer** | 物理/碰撞/射线分组（0–31 的数字索引） | 地形**必须** Layer 8 = `Terrain`，否则骑士穿地 |
| **Tag** | 贴给物体的字符串标签 | 游戏的 `GetTileMap` 要求 `CompareTag("TileMap")`（§4.1） |
| **Sorting Layer** | 2D 绘制顺序 | 设错就"人跑到背景后面" |

三者的定义都来自阶段二覆盖的 `ProjectSettings/TagManager.asset`。

### 3.4 AssetBundle 是什么

Unity 自己的打包格式：一个二进制文件，里面装着场景和它依赖的资源。
本流程里它被**塞进 `FinalMod/Resources/` 编译进 dll**，所以玩家只需要一个 dll 文件；
游戏启动时从 dll 里读出包（`AssetBundle.LoadFromStream`），进房间时按名字加载场景。

### 3.5 原版场景的门是怎么被"接管"的（为什么一边在 Unity 配、一边在 C# 配）

**先记住一句话：门的指向信息只存在"出发方"身上。**
`TransitionPoint` 的两个字段就是这个意思：

- `targetScene` = 踩到这道门之后**去哪个场景**
- `entryPoint` = 到了那个场景之后，**落在名叫这个的物体上**

目标场景**什么都不用配** —— 它只需要"有一个叫 entryPoint 那个名字的物体"
（这就是为什么房间里的 `left1` / `right1` 名字错一个字就落在虚空里）。

于是"从别的场景进我们房间"这件事，**必须写到原版那扇门上**。可原版场景你改不了：

| | 我们的房间场景 | 原版场景（Town / Crossroads_01 …） |
|---|---|---|
| 在哪 | 我们自己的 `.unity` 资源 | 游戏数据 `hollow_knight_Data\level*`（二进制） |
| 能不能用 Unity 打开 | ✅ | ❌ 打不开、也覆盖不了 |
| 门的指向怎么配 | **Unity 里静态配**（Inspector 填） | **运行时用 C# 改内存里的对象** |

**C# 那边具体是怎么做的**（对应你在 ModLog 里看到的那两行日志）：

```csharp
// 1) mod 初始化时注册：每当游戏加载一个新场景就会被调用
UnityEngine.SceneManagement.SceneManager.activeSceneChanged += OnSceneChanged;

// 2) 加载的是原版场景时，找到那扇门，改写它的两个字段
//    SceneChanger.RedirectVanillaGate(...)
GameObject gateGo = FindInScene(scene, "bot1");          // 递归找，含未激活物体
TransitionPoint tp = gateGo.GetComponent<TransitionPoint>();
tp.SetTargetScene("HKCS_Room01");                        // = tp.targetScene = "HKCS_Room01"
tp.entryPoint = "left1";
Modding.Logger.Log("[HKCS] Town.bot1 → HKCS_Room01:left1");   // ← 这行就是它干活的自证
```

> `SetTargetScene` 是原版 `TransitionPoint` 上的方法（反编译确认，方法体只有一句
> `this.targetScene = newScene;`），所以它跟直接赋值等价，没有隐藏魔法。

**两个容易踩的点**：

1. **`door#` 类型的门光改 `TransitionPoint` 不够**：那种门由 `Door Control` 这个 PlayMaker FSM 驱动，
   FSM 里还存着 `New Scene` / `Entry Gate` 两个变量，必须一起同步（`SceneChanger` 里就是这么写的）。
2. **双向共用一道门为什么不会被"弹回来"**：原版 `TransitionPoint` 有个字段 `lastEntered`，
   记录"刚刚从这个门进来"，游戏靠它避免立刻重新触发 —— 所以
   `Town.bot1 ↔ 房间 left1` 这种互指是成立的。
3. **⚠ 一个房间里可能有好几道门通向同一个场景，所以"按门名改一道"会漏**
   （2026-09-25 实测）：扫游戏数据得到 `Crossroads_01` 里的门是
   `(Town, bot1)`、`(Town, bot1)`、`(Crossroads_02, left1)`、`(Crossroads_07, right1)` ——
   **有两道门的目标都是 `Town`**。只改 `top2` 的结果就是"跳井能进房间，从井底爬上来照样回德特茅斯"。
   本项目因此改成**按目标场景扫**：`SceneChanger.RedirectAllGatesTo(scene, "Town", 房间, "right1")`，
   把该场景里所有指向 `Town` 的门一起接管，并逐道打印日志。

   > 副产品：**原版场景名 ↔ `levelN` 文件的映射**可以这样查 —— 在
   > `hollow_knight_Data\globalgamemanagers` 里按顺序提取 `Assets/Scenes/**.unity` 字符串，
   > 第 N 个就是 `hollow_knight_Data\levelN`（本机实测：`Town` = `level7`、`Crossroads_01` = `level37`）。
   > 再用"长度=字符数、4 字节对齐"的字符串格式扫 `(targetScene, entryPoint)` 相邻对，就能重建整个门图。

**如果原版没有合适的现成门可改**，也可以运行时**新建**一道：
`SceneChanger.CreateGateway(...)` 就是在原版场景里 `new GameObject` + `TransitionPoint` +
`BoxCollider2D` + `HazardRespawnMarker`。

> 这套「hook 住时机 → 改原版对象」就是 Mod 的通用套路；本项目只是把它用在门上。

---

## 4. 待补 / 已知缺口

### 4.1 `PatchTileMap`：唯一挡路的缺口

**缺什么**：官方教程的场景里有一个 `TileMap` 物体（Tag = `TileMap`）+ **`PatchTileMap`** 组件，
它运行时会 `AddComponent<tk2dTileMap>()` 并按房间尺寸初始化。
**SFCore 里没有这个类**（✅ 反编译 `Mods\SFCore\SFCore.dll` 确认：`SFCore.MonoBehaviours` 只有
`ArrayForEnumAttribute`、`BlurPlanePatcher`、`PatchMusicRegions`、`SceneManagerPatcher`、`SceneMapPatcher`、`SpritePatcher`），
**本仓库的壳工程和 `FinalMod/Patchers/` 也没有**（只有 3 个 Patch 组件）。

**为什么必须有**：`tk2dTileMap` 是**游戏里的类型**，Unity 编辑器里没有这个组件，所以没法在 Unity 里直接摆；
只能用一个 MonoBehaviour 在运行时 `AddComponent` 出来。

**✅ 反编译证据**（`Assembly-CSharp.dll`，Modding API v77）：

```csharp
// ① 游戏怎么找 tilemap：只在 Tag 匹配时取"同一个物体上"的 tk2dTileMap
tk2dTileMap GameManager.GetTileMap(GameObject go)
{
    if (!go.CompareTag("TileMap")) return null;
    return go.GetComponent<tk2dTileMap>();
}

// ② 主流程：三级回退，全失败只打一条 Error 然后 return（游戏自己不抛异常）
void GameManager.RefreshTilemapInfo(string scene)
{
    tilemap = 扫场景根物体(GetTileMap);            if (tilemap == null) {
    LogErrorFormat("Using fallback 1 to find tilemap. Scene {0} requires manual fixing.", scene);
    tilemap = 按 Tag "TileMap" 遍历 GetComponent<tk2dTileMap>();  if (tilemap == null) {
    LogErrorFormat("Using fallback 2 to find tilemap. Scene {0} requires manual fixing.", scene);
    tilemap = GameObject.Find("TileMap") 上 GetTileMap();          if (tilemap == null) {
    LogErrorFormat("Failed to find tilemap in {0} entirely.", scene);
    return;                                        // ← 到这里就结束，什么都不设
    }}}
    this.tilemap = tilemap;                        // tilemap 是自动属性
    this.sceneWidth  = tilemap.width;               // sceneWidth/sceneHeight 是 float
    this.sceneHeight = tilemap.height;
}
```

**真正抛异常的是我们自己的钩子** —— `FinalMod/SceneChanger.cs`：

```csharp
orig(self, targetScene);                       // ↑ 上面那段
RoomDef room = Rooms.Get(targetScene);
self.tilemap.width = (int)room.Width;          // ← tilemap 为 null → NullReferenceException
```

**症状**：进游戏日志同时出现 `Failed to find tilemap in HKCS_Room01 entirely.`
＋ 一条 `NullReferenceException`，且"尺寸设为 64x32"这行**不出现**。

**修法**（还没做）：补一个 `PatchTileMap`
—— 壳工程加"只有字段的空壳"（`width/height/columns/rows/partSizeX/partSizeY/physicsMaterial2D/renderData`），
`FinalMod` 加真实现（用 `tk2dTileMap` / `Layer` / `ColorChannel`），场景按第 12 步的**方案 B** 摆。

> 现在 FinalMod 已经能编译验证（第 19 步），所以这一步补完可以立刻连编带测。

### 4.2 数值不一致：`64×32` vs `30×17`

`RoomNames.cs` 里 `Room01` 登记的是 **64×32**，而示例地形 mesh 只有 **30×17**。
相机边界、场景边缘黑幕、游戏内地图都按表里的数字走，所以要么把地形做大/多摆几块，
要么把表里的数字改成 `30f, 17f`。**二选一，别放着不管。**

---

## 5. 修复记录（本会话已改的代码）

| 文件 | 改了什么 | 为什么 |
|---|---|---|
| `Assets\Materials\black.png`（新增） | 8×8 纯黑 PNG | 第 13 步 `Tex` 要的东西，Unity 里没法凭空新建贴图 |
| `UnityProject\Assets\Materials\black.png`（新增） | 同上（骨架拷贝源） | — |
| `FinalMod\Consts\RoomNames.cs` | `AreaInfo` 的 `AreaEvent`/`VisitedBool` 改成 `HKCustomSceneMod_AreaTitle` / `HKCustomSceneMod_VisitedArea` | 原本与 `CreateInitializer.cs`、`SettingsClass` 三处不一致 → 区域名空白、到访记录存不下来 |
| `FinalMod\HKCustomSceneMod.csproj` | ① `HollowKnightRefs` 指向真实游戏路径 ② `EmbeddedResource` 改条件式（认 `hkcs_scenes` 和 `my_first_assetbundle`）③ 删掉两个不存在的引用（`Modding.dll`、`UnityEngine.SceneManagementModule.dll`） | ① 默认路径在本机不存在 ② 包没打出来时编译会失败 ③ 这两个文件在 HK 的 `Managed\` 里**根本不存在** |
| `FinalMod\CustomSceneMod.cs` | `using SFCore.Generics;`；去掉 `using Modding;` 改按需别名；`SceneManager` 全限定；日志器传 `this` | `SaveSettingsMod<T>` 在 `SFCore.Generics`；`Logger`/`SceneManager` 与 UnityEngine / HK 自己的同名类型二义 |
| `FinalMod\SceneChanger.cs` | `using ILogger = Modding.ILogger;`；`SceneManager` 全限定 | 同上 |
| `FinalMod\PrefabHolder.cs` | `using ILogger = Modding.ILogger;` | 同上 |

> 冷知识：新版 Modding API 把 `Modding.*` 类型**并进了 `Assembly-CSharp.dll`**（全区找不到 `Modding.dll`），
> 而且 `Modding.IMod` 本身就继承 `ILogger` —— 所以 mod 自己就是日志器。

---

## 6. 排查总表

| 症状 | 原因 | 处理 |
|---|---|---|
| Add Component 搜不到 `PatchAreaTitleController` | 壳 dll 不在 `Assets\Assemblies\`，或工程有编译错误 | 先清 Console 红字；核对壳 dll 是否存在 |
| 组件显示 **`Missing (Mono Script)`** | 两个工程 AssemblyName / 命名空间 / 类名不一致 | 用 ILSpy 看 dll 里的真实全名（应为 `HKCustomSceneMod.Patchers.*`） |
| Layer 下拉里选不到 `Terrain` | 阶段二 `TagManager.asset` 没覆盖 | 关掉 Unity，覆盖 `ProjectSettings/TagManager.asset`，重开 |
| 角色穿地、掉出地图 | 地形 Layer 不是 8、或没加 2D 碰撞体、或加了 3D `MeshCollider` | 检查 Layer / `PolygonCollider2D` |
| **进房间后一直往下掉（黑背景直坠）** | ① 地形实例没有碰撞体（模型 prefab `addColliders: 0`）② 地形 Layer ≠ `Terrain` ③ **门的入口位置在地形范围之外** | 第 12 步第 3、4 条；第 15 步位置；Z 一律 0 |
| **日志 `PlayerData.SetHazardRespawn` NullReferenceException，入场流程被打断** | 门的 `TransitionPoint.respawnMarker` 是空的 | 第 15 步第 4 条：加 `HazardRespawnMarker` 子物体并拖进 `Respawn Marker` 槽 |
| **出房间后出现在原版场景上空然后坠落** | 门的 `Entry Point` 在该场景里不是那道"该落"的门（例如 `left1 → Town:top1`） | 按第 15 步 15.1 路线表改成 `left1 → Town:bot1` |
| **反方向进不了房间** | 只接管了一个方向，或两个方向共用同一个落点 | 见第 15 步 15.1：`Town.bot1` → `left1`、`Crossroads_01.top2` → `right1` |
| 日志 `场景包里找不到房间 HKCS_Room02/Room03` | `RoomNames.All` 登记了 3 间，包里只有 1 间 | 暂时把表里多余的房间删掉，或忽略（不影响 Room01） |
| 顶部菜单没有 `Build AssetBundles` | Editor 脚本没编译成功 | 清 Console 红字；确认 `Assets\Editor\CreateAssetBundles.cs` 在 |
| 打的包只有几百字节 | 场景没设 AssetBundle 名 | 回到第 16 步 |
| 日志 `场景包里找不到房间 HKCS_Room01` | 场景名 / 文件名 / `RoomNames.cs` 不一致，或包没重新打 | 三处对齐后重打包、重编译 |
| 日志 `Failed to find tilemap in ... entirely.` ＋ `NullReferenceException` | 缺 `PatchTileMap`（§4.1） | 补 `PatchTileMap` + 官方层级（第 12 步方案 B） |
| 进去后左上角没有区域名 | `AreaEvent`/`VisitedBool` 与 C# 侧不一致，或 `PatchAreaTitleController` 没挂 | 第 14 步字段表 + §5 的键名修复 |
| 摔死后重生在奇怪的地方 | 门的 `HazardRespawnMarker` 没挂/没连 | 第 15 步第 4 条 |
| 走到房间边缘相机不动、出现错误黑幕 | 房间尺寸没生效（`RefreshTilemapInfo` 失败） | 见 §4.1、§4.2 |
| 人跑到背景后面 | Sorting Layer 设错 | 按 `教学-从零理解.md` §4.6 的顺序表 |
| 编辑器里按 Play 报"找不到 shader tk2d/BlendVertexColor" | 正常现象 | 别在编辑器里测，进游戏测 |
